{
  "name": "lobs-memory",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch server/index.ts",
    "start": "bun run server/index.ts",
    "test": "bun test"
  },
  "dependencies": {
    "chokidar": "^4.0.0",
    "glob": "^10.0.0"
  },
  "devDependencies": {
    "bun-types": "^1.3.11",
    "typescript": "^5.5.0"
  }
}
