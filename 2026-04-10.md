- **[2026-04-10] [finding]** — Fixed inbox alert loop: LM Studio was reporting unreachable (stale inbox items), cleared 3 stale pending items (LM Studio diagnostic warnings). Live DB confirmed 0 pending unread items. The "93 unread" in heartbeat alerts was stale session context — heartbeat code itself is correct.
